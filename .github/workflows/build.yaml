name: Build and Push Docker Images

on:
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

permissions:
  contents: read
  packages: write

jobs:
  build-and-push-images:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log into registry ${{ env.REGISTRY }}
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push qreceive image
        uses: docker/build-push-action@v5
        with:
          context: .
          target: qreceive
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-qreceive:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-qreceive:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # - name: Build and push qmail image
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     target: qmail
      #     push: true
      #     tags: |
      #       ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-qmail:latest
      #       ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-qmail:${{ github.sha }}
      #     cache-from: type=gha
      #     cache-to: type=gha,mode=max
