services:
  qreceive:
    image: ghcr.io/driftwoodeval/questionnaires-qreceive:main
    environment:
      - HEADLESS=true
    volumes:
      - ./config:/app/config
      - ./put:/app/put
      - ./logs:/app/logs
    restart: unless-stopped
